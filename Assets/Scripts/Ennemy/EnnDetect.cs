using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnnDetect : MonoBehaviour
{
    /*
    //angle de vu ennemy
    public float fieldOfViewAngle = 110;
    // Le Player est-il dans la Zone ou non?
    public bool playerInZone;
    // Derniere position connue du player
    public Vector3 playerLastZone;

    // on utilise la longueur du chemin vers le player pour detection de bruit
    private NavMeshAgent nav;
    //on va la comparer au rayon du sphere collider qui suit
    private SphereCollider col;
    // Référence au script de dernière position connue du joueur
    private LastPlayerZone lastPlayerZone;
    // nous allons utiliser un ontrigger pour detect l'entree du player dans le sphere collider. Donc ref au player
    private GameObject player;
    // stocker la playerLastZone precedente. Pour savoir si sa position a changé.
    private Vector3 previousPlayerZone;


    void Awake()
    {
        nav = GetComponent<NavMeshAgent>();
        col = GetComponent<SphereCollider>();
        lastPlayerZone = GameObject.FindGameObjectsWithTag
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
